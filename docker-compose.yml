services:
  mcp-tools-server:
    build: .
    container_name: mcp-tools-server
    ports:
      - "8082:8082"
    environment:
      DATA_DIR: /app/data
      MCP_HOST: 0.0.0.0
      MCP_PORT: "8082"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
